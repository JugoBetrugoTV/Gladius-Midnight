<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.blizzard.com/wow/ui/ ..\FrameXML\UI.xsd">

    <!--
        Event Frame - Created in XML to avoid Midnight 12.0 protected function issues
        RegisterEvent is protected during combat - XML frames load at UI init before combat
        This is the same approach as sArena_Reloaded
    -->
    <Frame name="GladiusMidnightEventFrame" parent="UIParent" hidden="true">
        <Scripts>
            <OnLoad>
                -- Register all events at XML load time (before combat can occur)
                self:RegisterEvent("ARENA_OPPONENT_UPDATE")
                self:RegisterEvent("ARENA_PREP_OPPONENT_SPECIALIZATIONS")
                self:RegisterEvent("PLAYER_ENTERING_WORLD")
                self:RegisterEvent("ZONE_CHANGED_NEW_AREA")
                self:RegisterEvent("PLAYER_TARGET_CHANGED")
                self:RegisterEvent("UNIT_HEALTH")
                self:RegisterEvent("UNIT_MAXHEALTH")
                self:RegisterEvent("UNIT_POWER_UPDATE")
                self:RegisterEvent("UNIT_MAXPOWER")
                self:RegisterEvent("UNIT_SPELLCAST_SUCCEEDED")
                self:RegisterEvent("UNIT_AURA")
                self:RegisterEvent("UNIT_SPELLCAST_START")
                self:RegisterEvent("UNIT_SPELLCAST_STOP")
                self:RegisterEvent("UNIT_SPELLCAST_FAILED")
                self:RegisterEvent("UNIT_SPELLCAST_INTERRUPTED")
                self:RegisterEvent("UNIT_SPELLCAST_CHANNEL_START")
                self:RegisterEvent("UNIT_SPELLCAST_CHANNEL_STOP")
                self:RegisterEvent("PLAYER_REGEN_ENABLED")
                self:RegisterEvent("COMBAT_LOG_EVENT_UNFILTERED")
            </OnLoad>
            <OnEvent>
                -- Route events to addon callbacks (set up in Core.lua OnEnable)
                local callbacks = GladiusMidnightEventCallbacks
                if callbacks and callbacks[event] then
                    callbacks[event](event, ...)
                end
            </OnEvent>
        </Scripts>
    </Frame>

</Ui>
